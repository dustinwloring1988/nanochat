# Nanochat AI Scientist v2 — Work Checklist

**Status (2026-09-25):** Items 1–4, 6, and 7 are complete with compact evidence. Item 5, the security and human-only promotion boundary, is the remaining open gate. Raw experiment artifacts are removed after evidence capture; tracked results live under `evidence/`.

- [x] 1) **Production SFT quality evaluation** — Prepared the pinned local `smol-smoltalk` package (`f73fe857d519ff6ac5af2ea67c4d3834da7b8bcc`, Apache-2.0), 64 train and 32 held-out records with zero content overlap, a fixed 2,048-token budget of 16 steps and 65,536 held-out evaluation tokens, and numeric pass/stop thresholds. The clean final-checkpoint evaluation is recorded in `evidence/sft-quality-20260925.json`; BPB moved from `1.621042` to `1.848689`, so the predeclared decision is `inconclusive` and no quality claim or promotion was made.
- [x] 2) **Long-context curriculum proof** — Completed the approved two-step `2048→8192` proof with batch `1`, world size `1`, effective batch `8192`, gradient accumulation `[4, 1]`, and a `14 GiB` envelope. The real depth-6 model and ClimbMix loader passed forward/backward, checkpoint-boundary resume, and resource checks; peak allocated/reserved VRAM was `6,332,701,184`/`7,417,626,624` bytes at 8K. Evidence and explicit activation approval are recorded in `evidence/long-context-20260925.json` and `config/long_context_activation.json`. The default dynamic gate remains fail-closed; approval is limited to this bounded proof and does not authorize production promotion.
- [x] 3) **Real multi-stage BFTS descendant proof** — Completed a three-invocation lineage with literal `max_nodes=1`: an OpenRouter/qwen root, a stage-2 child, and a stage-3 child. Each child executed exactly one canonical node, inherited a verified parent artifact, and produced a controller attestation; compact evidence is in `evidence/ai-scientist-lineage-20260925.json`. Promotion remains unauthorized.
- [x] 4) **Search expansion and multi-seed confirmation** — Completed the sequential one-node protocol for seeds `[42, 43, 44]`. All three results are canonical and attested; mean held-out BPB is `1.494582` versus seed-42 `1.491984` (delta `0.002598`), and every seed is within the predeclared `+0.05` limit. Evidence is in `evidence/ai-scientist-seed-confirmation-20260925.json`; the node cap remains `1`.
- [ ] 5) **Security and promotion boundary** — Separate controller/generated execution, add OS-enforced private workspaces, and implement human-only signed promotion with clean reproduction.
- [x] 6) **Live provider expansion** — Completed a bounded OpenRouter run with persisted preflight/run evidence, `max_nodes=1`, 10-call/100,000-input/50,000-output caps, and explicit missing-usage opt-in. The accepted result and attestation are recorded in `evidence/ai-scientist-baseline-openrouter-20260925.json`; the earlier OpenCode pilot remains rejected.
- [x] 7) **License reorganization review** — Reviewed the preserved root MIT grant, separate `licenses/NANOCHAT_LICENSE`, and `licenses/AI_SCIENTIST_LICENSE`; updated `README.md` and `CHANGELOG.md` to describe the multi-license scope. The replacement license files are included in the change set.
